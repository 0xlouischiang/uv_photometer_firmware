within FiaSensor;
package Test
  "Standalone verification models, run via run_tests.mos. Each model is a
  thin wrapper (extends or wires a subset of the package) so a single
  simulate() call exercises exactly one claim -- see each model's own
  docstring for what it checks and why."
end Test;
